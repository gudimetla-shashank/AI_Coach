name: Scheduled API Test Runs

on:  
  workflow_dispatch:

jobs:
  run-api-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'  
          java-package: jdk

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run Tests with Maven
        run: mvn test

      - name: Generate Allure Report
        run: |
          mvn allure:report

      - name: List Allure Directory (for debugging)
        run: ls -R target/site/allure-maven-plugin

      - name: Upload Test Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: target/surefire-reports/*.xml

      - name: Upload Allure Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: target/site/allure-maven-plugin  # Verify if this path is correct

      - name: Send Email Notification
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: jobs619733@gmail.com
          password: genv yxfs gpty ekro
          subject: 'Test Results Notification'
          body: |
            The API tests have failed. Please check the details in the test reports.
          to: s96435493@gmail.com
          from: jobs619733@gmail.com
          attachments: |
            target/site/allure-maven-plugin/index.html
